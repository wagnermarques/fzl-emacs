(provide 'config_code_lisp)

;;https://web.archive.org/web/20141203143357/http://dto.github.io/notebook/require-cl.html#sec-8
;;    (loop for p in packages-to-be-installed-list uses it
(require 'cl)

;; the auto-revert-set-timer function needs it
(require 'timer)
