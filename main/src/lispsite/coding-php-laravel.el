(defun fzl-laravel-project-workdir-setup()
  "Sets current speedbar directory as a laravel project workdir. This workdir serves as a workdir for php artisan commands."
  
  )

(defun fzl-laravel-php-artisan-serve()
  

        
(provide 'coding-php-laravel)
