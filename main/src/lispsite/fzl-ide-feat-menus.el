;;; package --- Summary
;;; Commentary:
;;; Code:

(require 'easymenu)

;; NOTE: The legacy "HOST Config" and "Desktop" menus have been migrated
;; and consolidated into `desktoping-apps.el`.
;; All desktop launchers, cloud sync, host/system configs, and shortcuts
;; are now managed in `desktoping-apps.el`.

(provide 'fzl-ide-feat-menus)

;;; fzl-ide-feat-menus.el ends here
